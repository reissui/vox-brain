{
  "name": "brain-gw",
  "version": "0.1.0",
  "private": true,
  "description": "Optional Cloudflare gateway for a self-hosted remote Brain runner",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "check": "tsc",
    "test": "vitest run",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@cloudflare/workers-oauth-provider": "^0.8.1",
    "@modelcontextprotocol/sdk": "1.29.0",
    "agents": "^0.17.3",
    "zod": "^4.0.0"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "^0.18.3",
    "@cloudflare/workers-types": "^5.20260708.1",
    "typescript": "^5.9.3",
    "vitest": "^4.1.10",
    "wrangler": "^4.109.0"
  },
  "overrides": {
    "@hono/node-server": "^2.0.11",
    "zod-to-json-schema": {
      "zod": "$zod"
    }
  }
}
